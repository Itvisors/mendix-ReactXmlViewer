## ReactXmlViewer
XML viewer based on [react-xml-viewer](https://www.npmjs.com/package/react-xml-viewer)

## Features
- View XML formatted in human readable way
- Expand/collapse nodes
- Show line numbers
- Customizable theme

## Usage
- Place the widget on a page and connect the attribute with the XML content
- Optionally configure the other properties as desired
